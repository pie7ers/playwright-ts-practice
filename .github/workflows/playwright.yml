name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    env: 
      HEROKU_BASE_URL: ${{ vars.HEROKU_BASE_URL}}
      BASIC_AUTH_USER_GLOBAL: ${{ secrets.BASIC_AUTH_USER_GLOBAL}}
      BASIC_AUTH_PASS_GLOBAL: ${{ secrets.BASIC_AUTH_PASS_GLOBAL}}
      BASIC_AUTH_USER_CHROMIUM: ${{ secrets.BASIC_AUTH_USER_CHROMIUM}}
      BASIC_AUTH_PASS_CHROMIUM: ${{ secrets.BASIC_AUTH_PASS_CHROMIUM}}
      
    steps:
    - uses: actions/checkout@v4 
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - name: Run Playwright tests
      run: npx playwright test
    - uses: actions/upload-artifact@v4
      if: ${{ env.ACT != 'true' && !cancelled() }}
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30
